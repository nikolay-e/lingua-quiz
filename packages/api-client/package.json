{
  "name": "@lingua-quiz/api-client",
  "version": "0.0.0",
  "description": "Generated TypeScript client for the Lingua Quiz backend API",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist src/generated",
    "generate": "node ./generate.js",
    "prepare": "npm run generate && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.9.3",
    "openapi-typescript-codegen": "^0.29.0",
    "typescript": "^5.9.3"
  }
}
